[package]
name = "chuk-tool-runtime-isolation"
description = "OS-level sandbox backends for running untrusted code (Seatbelt, ...), for chuk-tool-runtime."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
tokio = { workspace = true, features = ["process", "rt", "io-util"] }

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt", "process", "io-util"] }
tempfile = "3"
